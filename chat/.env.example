# Copy to .env and fill in to wire the avatar (see app/main.py : _answer).
ANTHROPIC_API_KEY=
CHAT_MODEL=claude-haiku-4-5-20251001
# Where the harvested per-ruler JSON lives (repo data/ by default).
PAST_DATA_DIR=../data
